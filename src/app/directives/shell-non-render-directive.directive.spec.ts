import { ShellNonRenderDirectiveDirective } from './shell-non-render-directive.directive';

describe('ShellNonRenderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellNonRenderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
